{
    "tasks": {
        "dev": "deno run --allow-read --watch src/main.ts",
        "run": "deno run --allow-read src/main.ts",
        "test": "deno test --allow-read src/cart-reader/reader.test.ts",
        "test:watch": "deno task test --watch"
    },
    "imports": {
        "@std/assert": "jsr:@std/assert@1"
    },
    "lint": {
        "rules": {
            "exclude": ["prefer-const"]
        }
    }
}
